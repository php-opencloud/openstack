suites:
  default_suite:
    namespace: OpenStack
    psr4_prefix: OpenStack

formatter.name: pretty

extensions:
  - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  format:
    - html
    - clover
  output:
    - coverage
    - coverage.xml